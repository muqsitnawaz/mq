docs: add repo description summary

No code was changed in this task. This PR was opened by the wrapper after a read-only README inspection to confirm what the mq repo does.

## What changed

No source files were modified. The `.rush/pr.md` file was created as required by the task wrapper.

## Why

The task asked for a one-sentence repo description based on the README, then required writing this file so the wrapper can open a PR.

## How to test

No functional changes — nothing to test.
